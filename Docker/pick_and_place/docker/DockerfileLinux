FROM nvidia/cuda:11.0.3-runtime-ubuntu20.04
RUN apt-get update && apt-get install -y libcudnn8 
RUN apt-get install -y x11-apps
# Copy each directory explicitly to avoid workspace cruft

#VOLUME $PYTHON_WORKSPACE/src
# RUN pwd
COPY ./WCourseGUIBuildLinux2023.01.04v2 /linux_racer
#WCourseServerBuildLinux2023.01.01v1

# $DISPLAY=(Get-NetIPConfiguration).IPv4Address.IPAddress[1]
# docker run -ti --rm -e DISPLAY=$DISPLAY racelinux:latest
# docker run -ti -e DISPLAY=${DISPLAY}:0.0 racelinux:latest
# docker run -ti --rm -e DISPLAY=${DISPLAY}:0.0 racelinux:latest
ENV LD_LIBRARY_PATH=/usr/local/cuda/extras/CUPTI/lib64:/usr/local/cuda/lib:/usr/local/cuda/lib64
RUN ln -s /usr/local/cuda-11.0/targets/x86_64-linux/lib/libcusolver.so.10 /usr/local/cuda-11.0/targets/x86_64-linux/lib/libcusolver.so.11
#CMD xclock
#CMD ./linux_racer/w-course-linux.x86_64